import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  scrollview: {
    minHeight: "100%",
  },
  scrollviewContainer: {
    paddingBottom: 100,
  },
});
